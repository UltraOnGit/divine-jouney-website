const Footer = ({ text }) => {
    return (
        <footer className="footer">
            <p>{text}: Follow our development on social media soon!</p>
        </footer>
    );
}

export default Footer;
